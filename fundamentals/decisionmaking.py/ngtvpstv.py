#enter a program to read negative or positive

num=int(input("enter number:"))

if num > 0:

    print(f"positive{num}")

elif num < 0:

    print(f"negative{num}")

elif num == 0:

    print(f"zero{num}")    